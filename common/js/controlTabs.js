function initTabs(){	removeHref();}function createClickFunction(t, id){	$(t).click(function()	{		toggleTabs(id);	});}function toggleTabs(id){	var i = 0;	var len_i = $('.tabs li').length;	for (i; i < len_i; i++)	{		var tab = $('.tabs li')[i];		var t_id = tab.id;						if (t_id == id)		{						$(tab).addClass("on");			$("#" + t_id + "-Content").removeClass("hide").addClass("show");		}		else		{			$(tab).removeClass("on");			$("#" + t_id + "-Content").removeClass("show").addClass("hide");		}	}		var height = $("#" + id + "-Content").height() + 45
    $("#ctn-tabs .rc_top").height(height);
}function removeHref(){		var i = 0;	var len_i = $('.tabs li').length;		for (i; i < len_i; i++)	{		var tab = $('.tabs li')[i];		var tabLink = $('.tabs li a')[i];		var id = tab.id;		$(tabLink).removeAttr('href');		createClickFunction(tab, id);	}}