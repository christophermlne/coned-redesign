$(document).ready(function(){	initjQuery();});function initjQuery(){	    createRoundCorners();	initTabs();//	createLeftNavigationBg(); kal, i had to disable this to get the layerslider plugin to work. can you resolve the conflict?}function checkMaxValue(num, maxnum){	if (num == maxnum)	{		return true;	}}function checkMinValue(num, minnum){	if (num == minnum)	{		return true;	}}